# Set dev

## set docker

- image build

```bash
docker-compose build --no-cache
```

- run docker

```bash
docker-compose up
```

- access to container

```bash
docker-compose exec api bash
```

- run server

```bash
python server.py
```
